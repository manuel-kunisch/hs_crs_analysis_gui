# Synthetic quickstart

This example will provide a small synthetic dataset that can be used to test installation and basic GUI behavior without requiring experimental data.

## Planned content

- Small TIFF stack with known component spectra.
- Matching `wavelength.json`.
- Preset with component labels and colors.
- Expected NNMF or fixed-H NNLS result.
- Exported reference TIFF and CSV files.

## Purpose

Use this example to check:

- Data loading.
- Spectral axis loading.
- ROI manager labels.
- Fixed-H NNLS behavior.
- Fiji/ImageJ export.
- Preset loading and saving.

> Placeholder: add synthetic dataset download link.

> GIF placeholder: run the synthetic quickstart from load to export.
