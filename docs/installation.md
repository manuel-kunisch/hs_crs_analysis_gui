# Installation

TODO: Move the installation details from the README into a full installation guide.

Include:

- Conda setup with `environment.yml`.
- Optional PyTorch setup with `environment-pytorch.yml`.
- CUDA notes for NVIDIA GPUs.
- Windows batch launcher usage.
- Common installation problems.
