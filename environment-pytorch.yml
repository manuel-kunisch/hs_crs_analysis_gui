name: hs-mv-analysis-pytorch
channels:
  - pytorch
  - conda-forge
dependencies:
  - python>=3.11
  - numpy>=1.26,<3
  - scipy>=1.11,<2
  - scikit-image>=0.22,<1
  - scikit-learn>=1.4,<2
  - tifffile>=2024,<2027
  - matplotlib>=3.8,<4
  - pyqt=5.15.*
  - pyqtgraph>=0.13,<1
  - qtawesome>=1.3,<2
  - pytorch>=2.2,<3
  - pip
# This is the general PyTorch environment file.
# If someone specifically wants CUDA-enabled PyTorch, they may still want to
# adjust the environment using the official PyTorch install selector for their GPU.
