name: hs-mv-analysis
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - numpy>=1.26,<3
  - scipy>=1.11,<2
  - scikit-image>=0.22,<1
  - scikit-learn>=1.4,<2
  - tifffile>=2024,<2027
  - matplotlib>=3.8,<4
  - pyqt=5.15.*
  - pyqtgraph>=0.13,<1
  - qtawesome>=1.3,<2
  - pip
# Optional:
#   Install PyTorch separately if you want the optional GPU/torch NNMF and NNLS paths.
#   Example after env creation:
#     pip install torch
