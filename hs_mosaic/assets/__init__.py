"""Bundled binary assets (icons, example metadata) for HS-MOSAIC.

Resources here are looked up via ``importlib.resources`` so the same code
works in editable installs, wheel installs, and PyInstaller frozen builds.
"""
